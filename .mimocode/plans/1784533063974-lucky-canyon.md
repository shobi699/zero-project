# 3 STT Modes — Settings Integration with Faster-Whisper

## Goal
Add 3 selectable STT modes in Settings, each with its own architecture:

| Mode | Tech | Use Case |
|------|------|----------|
| **حالت ۱: مرورگر (سبک)** | Web Speech API | رایگان، سریع، بدون دانلود |
| **حالت ۲: مدل لوکال (آفلاین)** | whisper.cpp + GGML models | بدون اینترنت، فارسی دقیق |
| **حالت ۳: پیشرفته (سریع)** | Faster-Whisper (Python) | دقت بالا، GPU اختیاری |

## Current State
- Settings.tsx: 3 radio buttons (hybrid/cloud/local) — need redesign
- Notepad.tsx: Web Speech API already working
- Daemon: whisper.cpp local engine working
- Config: `engine_mode` field exists

## Files to Modify

### 1. `zero-studio/src/components/Settings.tsx` — Redesign engine selection

Replace the old 3 radio buttons with the new 3 STT modes:

```
┌─ حالت تبدیل گفتار (STT Mode) ─────────────────────────┐
│                                                          │
│ ┌─ حالت ۱: مرورگر / سبک ────────────────────────────┐  │
│ │ ✓ Web Speech API — صفر هزینه، صفر دانلود           │  │
│ │   پیشنهادی برای استفاده روزمره                       │  │
│ │   نیاز به Edge/Chrome — پشتیبانی فارسی عالی        │  │
│ └────────────────────────────────────────────────────┘  │
│                                                          │
│ ┌─ حالت ۲: مدل لوکال / آفلاین ──────────────────────┐  │
│ │   whisper.cpp — بدون اینترنت                        │  │
│ │   نیاز به دانلود مدل (75MB-1.5GB)                  │  │
│ │   پشتیبانی فارسی با مدل‌های C1Tech                  │  │
│ └────────────────────────────────────────────────────┘  │
│                                                          │
│ ┌─ حالت ۳: پیشرفته / سریع ──────────────────────────┐  │
│ │   Faster-Whisper — دقت بالا + سرعت بالا            │  │
│ │   نیاز به Python + faster-whisper                   │  │
│ │   GPU اختیاری (10x سریع‌تر با CUDA)                │  │
│ └────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

Each mode shows:
- Status indicator (فعال / غیرفعال / نیاز به نصب)
- Quick action button (فعال کردن / نصب / تست)

### 2. `tools/faster-whisper-server/server.py` — NEW: Python HTTP server

Simple FastAPI server that accepts audio and returns text:

```python
# POST /transcribe — accepts WAV audio, returns JSON { "text": "..." }
# GET /health — returns server status
# GET /models — returns available models
# Port: 8787
```

Dependencies: `pip install faster-whisper fastapi uvicorn`

### 3. `zero-studio/src-tauri/src/main.rs` — Faster-Whisper management commands

```rust
#[tauri::command]
async fn check_faster_whisper() -> Result<serde_json::Value, String>
// Checks if Python + faster-whisper are installed, server running

#[tauri::command]
async fn start_faster_whisper() -> Result<String, String>
// Starts the Python server as a child process

#[tauri::command]
async fn stop_faster_whisper() -> Result<String, String>
// Stops the Python server

#[tauri::command]
async fn install_faster_whisper() -> Result<String, String>
// Runs pip install faster-whisper fastapi uvicorn
```

### 4. `zero-daemon/src/config.rs` — Add `stt_mode` field

```rust
pub struct DaemonConfig {
    // ... existing fields ...
    pub stt_mode: String,  // "browser" | "local" | "faster-whisper"
}
```

### 5. `zero-studio/src/components/Notepad.tsx` — Mode-aware voice input

The notepad should use the selected mode:
- **browser**: Web Speech API (already implemented)
- **local**: Call daemon's `record_for_notepad` IPC
- **faster-whisper**: Send audio to Faster-Whisper server via HTTP

### 6. `zero-studio/src/components/ModelManager.tsx` — Show relevant models per mode

- Mode browser: no models needed
- Mode local: show GGML models (whisper.cpp)
- Mode faster-whisper: show HuggingFace models (C1Tech Persian, etc.)

---

## Implementation Order

1. **Config** — Add `stt_mode` to daemon config
2. **Settings UI** — Redesign with 3 modes + status indicators
3. **Faster-Whisper server** — Create Python server script
4. **Tauri commands** — Add check/start/stop/install commands
5. **Notepad** — Make voice input mode-aware
6. **ModelManager** — Show relevant models per mode

---

## Verification

1. Settings shows 3 modes with correct status indicators
2. Selecting "مرورگر" → notepad uses Web Speech API
3. Selecting "لوکال" → notepad uses daemon IPC
4. Selecting "پیشرفته" → Faster-Whisper server starts, notepad uses HTTP
5. Each mode's test button works correctly
6. Config persists across app restarts
